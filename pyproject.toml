[build-system]
requires = ["setuptools>=34.4", "wheel"]
build-backend = "setuptools.build_meta"

[tool.semantic_release]
version_variable = ["lightkde/__init__.py:__version__"]
branch = "main"

[tool.coverage.run]
source = ["lightkde"]
